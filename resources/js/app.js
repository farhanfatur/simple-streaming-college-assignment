require('./bootstrap');
import { 
    Alert,
    Datepicker, 
    Collapse,
    Dropdown, 
    Modal,
    Ripple,
    Input, 
    initTE } from "tw-elements";
initTE({ Alert, Collapse, Modal, Ripple, Dropdown, Datepicker, Input });
